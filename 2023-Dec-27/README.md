# "Fastest" power calculator

The intention was to create a calculator that calculated the power (like `x = pow(a,b)` or `x = a ** b` using the least number of multiplications

Actual speed was not measured
